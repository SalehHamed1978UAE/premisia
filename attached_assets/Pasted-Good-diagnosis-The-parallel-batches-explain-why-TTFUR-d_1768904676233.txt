Good diagnosis. The parallel batches explain why TTFUR didn't improve much -  
  all batches complete nearly simultaneously.                                   
                                                                                
  Before implementing hybrid approach, clarify the tradeoff:                    
  ┌────────────────────────────────────┬───────┬───────────────┬───────────────┐
  │              Approach              │ TTFUR │ Total Scoring │ Total Journey │
  ├────────────────────────────────────┼───────┼───────────────┼───────────────┤
  │ Current (full parallel)            │ ~174s │ ~48s          │ ~227s         │
  ├────────────────────────────────────┼───────┼───────────────┼───────────────┤
  │ Hybrid (1 sequential + 3 parallel) │ ~157s │ ~60s          │ ~240s         │
  └────────────────────────────────────┴───────┴───────────────┴───────────────┘
  The 174s TTFUR in logs is actually promising - that's 21s faster than the 195s
   baseline. The question is: does the frontend actually RENDER the             
  partial_scores data at 174s?                                                  
                                                                                
  Check before changing scoring approach:                                       
  1. Does the frontend show genome preview cards when partial_scores arrives?   
  2. Or does it only show a text message like "50/89 scored"?                   
                                                                                
  If the frontend already renders top genomes at 174s (from first batch), then  
  TTFUR is already improved - just not reflected in user perception because the 
  UI isn't surfacing it visibly.                                                
                                                                                
  Recommendation: Before adding complexity with hybrid sequential/parallel      
  scoring, verify the frontend actually displays the partial genome data. The   
  174s event might already be the win - it just needs UI visibility.            
                                                                                
  If the frontend only shows progress text (not genome cards), fix that first.  
  Adding hybrid scoring adds 10-15s to total time which may not be worth it. 